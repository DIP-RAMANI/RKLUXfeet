<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    tools:context=".AdminOrdersActivity">

    <!-- Header Frame -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clOrdersHeader"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:background="@drawable/bg_home_header"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/ivOrdersBack"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="20dp"
            android:layout_marginTop="32dp"
            android:src="@drawable/ic_arrow_back"
            app:tint="#000000"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvOrdersTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:fontFamily="sans-serif-black"
            android:text="Manage Orders"
            android:textColor="#000000"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/ivOrdersBack"
            app:layout_constraintStart_toEndOf="@id/ivOrdersBack"
            app:layout_constraintTop_toTopOf="@id/ivOrdersBack" />

        <TextView
            android:id="@+id/tvOrderCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="20dp"
            android:text="0 pending"
            android:textColor="#555555"
            android:textSize="13sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/ivOrdersBack"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/ivOrdersBack" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Search Bar -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cvOrdersSearch"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginHorizontal="20dp"
        app:cardCornerRadius="25dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@id/clOrdersHeader"
        android:layout_marginTop="-25dp">
        
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="16dp">
            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_search"
                app:tint="#8D8D8D"/>
            <EditText
                android:id="@+id/etOrdersSearch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@null"
                android:hint="Search customer or product..."
                android:textColor="#000000"
                android:textColorHint="#8D8D8D"
                android:textSize="14sp"
                android:layout_marginStart="10dp"
                android:singleLine="true"
                android:imeOptions="actionSearch" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Filter Chips Row -->
    <HorizontalScrollView
        android:id="@+id/hsvOrdersFilter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/cvOrdersSearch">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="20dp"
            android:gravity="center_vertical">
            <TextView android:id="@+id/chipFilterAll"       android:layout_width="wrap_content" android:layout_height="36dp" android:text="All"         android:gravity="center" android:paddingHorizontal="18dp" android:textSize="13sp" android:background="@drawable/bg_chip_active"   android:textColor="#FFFFFF" android:layout_marginEnd="8dp" />
            <TextView android:id="@+id/chipFilterPending"   android:layout_width="wrap_content" android:layout_height="36dp" android:text="Pending"     android:gravity="center" android:paddingHorizontal="18dp" android:textSize="13sp" android:background="@drawable/bg_chip_inactive" android:textColor="#000000" android:layout_marginEnd="8dp" />
            <TextView android:id="@+id/chipFilterShipped"   android:layout_width="wrap_content" android:layout_height="36dp" android:text="Shipped"     android:gravity="center" android:paddingHorizontal="18dp" android:textSize="13sp" android:background="@drawable/bg_chip_inactive" android:textColor="#000000" android:layout_marginEnd="8dp" />
            <TextView android:id="@+id/chipFilterDelivered" android:layout_width="wrap_content" android:layout_height="36dp" android:text="Delivered"   android:gravity="center" android:paddingHorizontal="18dp" android:textSize="13sp" android:background="@drawable/bg_chip_inactive" android:textColor="#000000" android:layout_marginEnd="8dp" />
            <TextView android:id="@+id/chipFilterCancelled" android:layout_width="wrap_content" android:layout_height="36dp" android:text="Cancelled"   android:gravity="center" android:paddingHorizontal="18dp" android:textSize="13sp" android:background="@drawable/bg_chip_inactive" android:textColor="#000000" android:layout_marginEnd="8dp" />
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Orders List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvOrders"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:paddingHorizontal="8dp"
        android:clipToPadding="false"
        android:paddingBottom="24dp"
        app:layout_constraintTop_toBottomOf="@id/hsvOrdersFilter"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
